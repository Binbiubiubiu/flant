import 'package:flutter/material.dart';

class PageTheme {
  static const padding = EdgeInsets.only(
    top: 46.0,
    left: 20.0,
    right: 20.0,
    bottom: 20.0,
  );

  static const subTextColor = Color.fromRGBO(69, 90, 100, 0.6);
}
